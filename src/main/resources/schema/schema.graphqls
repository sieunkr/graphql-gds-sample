type Query {
    singers(name: String): [Singer]
    singersBySameAge(name: String): [Singer]
}

type Singer {
    name: String
    age: Int
    followers: [Follower]
}

type Follower {
    nick: String
}